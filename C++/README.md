tips

# Debug tip

- 디버그 - '디스어셈블리'를 가면 어셈블리 언어를 볼 수 있다.

- 디버그 - '메모리'를 통해 실제로 메모리에 저장된 값들을 볼 수 있다.

  Low Level을 볼 때 유용함

- VS 에서는 Ctrl k + C (Comment), Ctrl k + u(UnComment)로 할 수 있다.

- CTRL ART D 를 통해서 어셈블리 영역 디버깅 가능하다.
